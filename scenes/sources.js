// GENERATED by tools/build_player_sources.py — do not edit by hand.
window.SCENES = {
  "butterfly": { title: "A butterfly on the Temple Fay curve r = e^sin \u03b8 \u2212 2 cos 4\u03b8 + sin\u2075((2\u03b8\u2212\u03c0)/24),", code: "// ==== ORIGINAL SKETCH \u2014 \"butterfly\" ====\n// A butterfly on the Temple Fay curve r = e^sin \u03b8 \u2212 2 cos 4\u03b8 + sin\u2075((2\u03b8\u2212\u03c0)/24),\n// flapping by squashing the wings horizontally with cos 2t, plus a dotted\n// body, antennae, and edge sparkle.\nG = 2.39996  // golden angle\n\nr = h => exp(sin(h)) - 2 * cos(4 * h) + pow(sin((2 * h - PI) / 24), 5)\n\nt = 0\ndraw = $ => {\n  t || createCanvas(w = 400, w)\n  background(9).stroke(w, 96)\n  t += PI / 40\n  F = 0.62 + 0.38 * cos(2 * t)          // flap: 1 open .. 0.24 folded\n  cx = 200\n  cy = 205 + 6 * sin(t)                 // gentle hover bob\n  for (i = 2e4; i--;) {\n    if (i % 8 < 6) {\n      // wing membrane: fill the curve, density pushed toward the edge\n      h = 24 * PI * ((i * G) % 1)\n      q = r(h) * (0.3 + 0.7 * pow((i / 15000) % 1, 0.35))\n      x = 40 * q * sin(h)\n      y = -34 * q * cos(h)\n      point(cx + x * F + sin(i * 47),\n            cy + y + 0.22 * (1 - F) * abs(x) + cos(i * 31))   // wings tilt up as they fold\n    } else if (i % 8 == 6) {\n      // edge sparkle: bright rim right on the curve\n      h = 24 * PI * ((i / 2500) % 1)\n      q = r(h)\n      point(cx + 40 * q * sin(h) * F, cy - 34 * q * cos(h) + 0.22 * (1 - F) * abs(40 * q * sin(h)))\n    } else {\n      // body + antennae\n      u = (i / 999) % 1\n      side = (i >> 3) % 2 ? 1 : -1\n      if ((i >> 4) % 3) {\n        // body: slim vertical ellipse of dots\n        point(cx + 4 * cos(i * G) * (1 - 0.5 * u) + sin(i * 43),\n              cy - 60 + 105 * u)\n      } else {\n        // antennae: two arcs curling out of the head\n        point(cx + side * (3 + 26 * u - 8 * u * u) + cos(i * G),\n              cy - 62 - 30 * u + 18 * u * u + 1.5 * sin(t + 3 * u))\n      }\n    }\n  }\n}\n// ==== END SKETCH ====\n\nWATERMARK = 'Byamba Enkhbat'\nFORMULA = [\n  'r(\u03b8) = e^sin \u03b8 \u2212 2 cos 4\u03b8 + sin\u2075((2\u03b8 \u2212 \u03c0)/24)',\n  'wing = (40 r sin \u03b8 \u00b7 F,  \u221234 r cos \u03b8),  F = 0.62 + 0.38 cos 2t',\n]\n\n// t coefficients {1, 2}, dt = PI/40  ->  full period 2*PI = 80 draw-steps.\nloop(80, 1)\n" },
  "dragon": { title: "A serpentine dragon: spine curve with a traveling wave, ribbed tube body,", code: "// ==== ORIGINAL SKETCH \u2014 \"dragon\" (Byamba Enkhbat) ====\n// A serpentine dragon: spine curve with a traveling wave, ribbed tube body,\n// sawtooth dorsal spikes, horns and whiskers at the head.\nG = 2.39996  // golden angle, decorrelates ring particles\n// spine: s = 0 head .. 1 tail tip (s < 0 = snout)\nP = s => [\n  330 - 288 * s + 10 * s * sin(2 * s + 2 * t),\n  140 + 130 * s + (14 + 44 * s) * sin(6 * s - t)\n]\n// body radius: slender tube + gaussian head bulge, snout taper below s=0\nR = s => s < 0 ? 6 * (1 + s / 0.06)\n              : 2.5 + 15 * sin(PI * (0.06 + 0.94 * s)) * (1 - 0.55 * s)\n                    + 8 * exp(-(s - 0.03) * (s - 0.03) / 0.004)\n\nt = 0\ndraw = $ => {\n  t || createCanvas(w = 400, w)\n  background(9).stroke(w, 96)\n  t += PI / 40\n  for (i = 2e4; i--;) {\n    if (i % 8 < 6) {\n      // body tube: rings of points around the spine (ribs / scales)\n      s = -0.06 + 1.06 * ((i / 15000) % 1)\n      p = P(s); p2 = P(s + 0.01)\n      dx = p2[0] - p[0]; dy = p2[1] - p[1]; L = mag(dx, dy) + 1e-6\n      ring = cos(i * G) * R(s)\n      jit = 2 * sin(i * 47)\n      point(p[0] - dy / L * ring + dx / L * jit,\n            p[1] + dx / L * ring + dy / L * jit)\n    } else if (i % 8 == 6) {\n      // dorsal spikes: sawtooth fins along the back\n      s = 0.1 + 0.85 * ((i / 2500) % 1)\n      f = (s * 16) % 1\n      p = P(s)\n      h = (10 + 15 * sin(PI * s)) * (1 - f)\n      point(p[0] + 2 * sin(i), p[1] - R(s) * 0.9 - h * ((i * G) % 1))\n    } else {\n      // head gear \u2014 i%8==7 fixes the low bits, so derive role/side from\n      // higher bits: role 0 = horns, 1 = whiskers, 2 = eye, 3 = tail tuft\n      u = (i / 999) % 1\n      side = (i >> 3) % 2 ? 1 : -1\n      role = (i >> 4) % 4\n      th = cos(i * G) * 2 * (1 - 0.6 * u)\n      if (role == 2) {\n        hd = P(0.02)\n        r = 2 * sqrt((i * G) % 1)\n        point(hd[0] + 5 + r * cos(i * G * 7), hd[1] - 3 + r * sin(i * G * 7))\n      } else if (role == 1) {\n        st = P(-0.045)\n        point(st[0] + 4 + 8 * u - 20 * u * u + th * 0.8,\n              st[1] + 4 + side * (1 + 2 * u) + 14 * u + 3 * u * sin(6 * u - t) + th)\n      } else if (role == 0) {\n        bp = P(0.035)\n        point(bp[0] - 2 - 16 * u - 8 * u * u + side * (2 + 3 * u) + th,\n              bp[1] - R(0.035) * 0.8 - 14 * u + 9 * u * u + th * 0.8 + 1.5 * sin(t + 2 * u))\n      } else {\n        tp = P(0.985)\n        point(tp[0] - 4 - 20 * u + th,\n              tp[1] + side * u * (3 + 8 * u) + 4 * u * sin(6 * u - t) + th)\n      }\n    }\n  }\n}\n// ==== END SKETCH ====\n\nWATERMARK = 'Byamba Enkhbat'\nFORMULA = [\n  'spine(s) = (330 \u2212 288s,  140 + 130s + (14 + 44s) sin(6s \u2212 t))',\n  'body     = spine \u00b1 n\u0302 R(s) cos(i\u03c6),  R = 2.5 + 15 sin \u03c0s (1 \u2212 0.55s) + 8e^(\u2212(s\u2212.03)\u00b2/.004)',\n  'spikes   = (10 + 15 sin \u03c0s) \u00d7 (1 \u2212 frac 16s)',\n]\n\n// t coefficients {1, 2}, dt = PI/40  ->  full period 2*PI = 80 draw-steps.\nloop(80, 1)\n" },
  "jellyfish": { title: "A moon jelly: breathing bell (dome of golden-angle rings), scalloped rim", code: "// ==== ORIGINAL SKETCH \u2014 \"jellyfish\" ====\n// A moon jelly: breathing bell (dome of golden-angle rings), scalloped rim\n// frill, and nine trailing tentacles waving out of phase.\nG = 2.39996  // golden angle, decorrelates particles\n\nt = 0\ndraw = $ => {\n  t || createCanvas(w = 400, w)\n  background(9).stroke(w, 96)\n  t += PI / 40\n  B = 1 + 0.12 * sin(t)                  // breath: wide-squash factor\n  cx = 200\n  cy = 138 + 8 * sin(t - 1.2)            // bob lags the breath (jet propulsion)\n  for (i = 2e4; i--;) {\n    if (i % 5 < 3) {\n      // bell dome: nested membrane arcs, all anchored at the rim corners\n      u = -1 + 2 * ((i * G) % 1)                   // horizontal param\n      sh = 0.68 + 0.32 * ((i * 2.2360679) % 1) + 0.03 * sin(i * 7.3)\n      point(cx + 95 * u * B + 1.5 * sin(i * 47),\n            cy - 76 * sh * pow(abs(1 - u * u), 0.62) * (2 - B) + 1.5 * cos(i * 31))\n    } else if (i % 5 == 3) {\n      // rim frill: scalloped curtain hanging from the bell's lower edge\n      u = -1 + 2 * ((i / 4000) % 1)\n      f = (9 * abs(sin(6 * u + t)) + 3) * ((i * G) % 1)\n      point(cx + 95 * u * B + sin(i * 53),\n            cy + 1 + 5 * u * u + f)\n    } else {\n      // tentacles: nine strands of varied length, waves travel downward\n      k = i % 9\n      s = (i / 2200) % 1                            // 0 root .. 1 tip\n      L = 0.7 + 0.3 * sin(k * 2.7)\n      point(cx + (k - 4) * 19 * B * (1 - 0.3 * s)\n              + (6 + 34 * s) * sin(5 * s - t + 0.9 * k) * (0.2 + 0.8 * s)\n              + 1.5 * sin(i * 59),\n            cy + 6 + 215 * s * L + 7 * s * sin(t + k))\n    }\n  }\n}\n// ==== END SKETCH ====\n\nWATERMARK = 'Byamba Enkhbat'\nFORMULA = [\n  'bell      = (95u\u00b7B,  \u221276\u00b7sh\u00b7(1 \u2212 u\u00b2)^.62 \u00b7 (2 \u2212 B)),  B = 1 + 0.12 sin t',\n  'frill     = (9 |sin(6u + t)| + 3) \u00b7 frac(i\u03c6)',\n  'tentacle  = (6 + 34s) \u00b7 sin(5s \u2212 t + 0.9k)',\n]\n\n// t coefficients {1}, dt = PI/40  ->  full period 2*PI = 80 draw-steps.\nloop(80, 1)\n" }
};
